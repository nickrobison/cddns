{
    "source": {
        "id": "ipify",
        "name": "Default",
        "config": {
            "refresh": 2
        }
    },
    "targets": [{
        "id": "logging",
        "name": "Test Logger",
        "config": {}
    }]
}
